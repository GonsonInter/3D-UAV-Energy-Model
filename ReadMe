本程序用于无人机三维能耗模型的实验


程序入口：Test.java
首先要定义一张Map，并且设置障碍物，设置AP的覆盖范围（如果不设置，默认为全覆盖）

（设置输出文件的名字）

设置完成后开始运行，控制台输出finish后在桌面的experiment文件夹会生成一个.gtsp文件，这个文件之后将送去GLNS求解器求解。

************************************************************************************************************************
在GLNS求解器求解，生成一个路径文件。
************************************************************************************************************************

进入genWidgetPath.java
在main函数中设置输出文件名，点击运行，控制台输出两个finish后会在桌面experiment文件夹生成两个文件。一个是Map的信息，一个是Widget路径序列。
到此此程序结束。生成的两个文件会送去python程序进行可视化。

************************************************************************************************************************
